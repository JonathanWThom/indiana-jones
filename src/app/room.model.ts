export class Room {
  constructor(public name: string, public content: string, public id: number, public userEffect: number, public optionOne: number, public optionTwo: number){}

  // add options soon
}
